name: web-crawl4ai
version: 1.0.0
description: "Self-hosted web search + extraction. No API key required. Uses httpx + trafilatura for static pages and Crawl4AI for JS-rendered content. DuckDuckGo HTML scraping for search."
author: NousResearch
kind: backend
provides_web_providers:
  - crawl4ai